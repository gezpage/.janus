Vim plugins for use with Janus Vim Distribution

From home folder, execute:

git clone --recursive https://github.com/gezpage/.janus.git

The recursive parameter will git pull each submodule and provided Janus is correctly installed Vim will automatically load the plugins
